
\section{Iostream}
%keywords:for,fori,forint,forloop

\begin{verbatim}
for (size_t i = 0; i < .size(); i++) {
    
}
\end{verbatim}

\section{Iostream}
%keywords:io,ios

\begin{verbatim}
#include <iostream>
\end{verbatim}

\section{Regular Includes}
%keywords:include,includes,incl

\begin{verbatim}
#include <iostream>
#include <vector>
#include <fstream>
#include <filesystem>

int main(int argc, char* argv[]) {

    const std::vector<std::string> arguments(argv + 1, argv + argc);
    const auto number_of_arguments = arguments.size();
    
    if (number_of_arguments == 0) {
        std::cout << "No arg provided\n"; 
    } else {
        const auto first_argument = arguments[0];
    }

    return 0;
}
\end{verbatim}
