
std::vector<char> vowels = {'a', 'e', 'i', 'o', 'u'};

for (index = 0; index < vowels.size(); index++) {
    std::cout << vowels[index] << "\n";
}


std::vector<char> vowels = {'a', 'e', 'i', 'o', 'u'};

for (index = 0; index < vowels.size(); index++) {
    std::cout << vowels[index] << "\n";
}

