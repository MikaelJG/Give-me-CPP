# Create source file and CMakeLists.txt
#
# mkdir build directory
#
# cd build dir.
#
# cmake .. ---- specify that the source file is in parent directory
#           --- Generates the Build Files / Configure the project
#
# cmake --build .  ---- creates the executable
#
# ./Executable , run the executable
